<%- include('partials/header', { title: "Images" }) %>

<div class="container my-5">
    <% if (images.length === 0) { %>
        <div class="alert alert-light border rounded text-center shadow-sm py-5" role="alert">
            <h2 class="mb-3">📸 Welcome to Image Upload App</h2>
            <p class="lead">Start uploading your beautiful images and build a gallery!</p>
            <a href="/add" class="btn btn-outline-primary btn-lg mt-3">Upload Images</a>
        </div>
    <% } else { %>
        <div class="text-center py-5 bg-light rounded shadow-sm">
            <h2 class="mb-4">📸 Gallery Preview</h2>
            <p class="lead">We found some amazing images uploaded already. Go check them out!</p>
            <a href="/gallery" class="btn btn-primary btn-lg mt-3">View Gallery Page</a>
        </div>
    <% } %>
</div>